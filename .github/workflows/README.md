# .github

Esta pasta contém as automações do projeto via GitHub Actions.

## workflows

### deploy.yml
Responsável por gerar a build do projeto e publicar a aplicação.

### update-prices.yml
Responsável por rodar o script de atualização de preços, salvar os dados atualizados e manter o site sincronizado.